<?xml version="1.0" encoding="utf-8"?>
<resources>
    <dimen name="dialpad_button_size">68dp</dimen>
    <dimen name="dialpad_button_size_small">50dp</dimen>
    <dimen name="incoming_call_arrow_size">50dp</dimen>
    <dimen name="incoming_call_button_size">72dp</dimen>
    <dimen name="call_notification_button_size">40dp</dimen>
    <dimen name="caller_avatar_max_size">240dp</dimen>
    <dimen name="end_call_button_margin">34dp</dimen>
    <dimen name="dialpad_bottom_padding">114dp</dimen>
    <dimen name="call_control_max_height">240dp</dimen>
    <dimen name="call_control_button_size">72dp</dimen>
    <dimen name="recyclerview_fab_padding">100dp</dimen>

    <dimen name="caller_name_text_size">34sp</dimen>
    <dimen name="call_status_text_size">20sp</dimen>

    <!-- keep these 2 values in dp to avoid scaling -->
    <dimen name="dialpad_text_size">28dp</dimen>
    <dimen name="dialpad_letters_text_size">12dp</dimen>

    <!-- ── Call Screen extras ─────────────────────────────────────────── -->
    <!-- Avatar sizes for active vs. waiting call -->
    <dimen name="call_avatar_size_large">80dp</dimen>
    <dimen name="call_avatar_size_small">52dp</dimen>

    <!-- Corner radius for call-waiting card (Material 3 ExtraLarge shape) -->
    <dimen name="call_card_corner_radius">20dp</dimen>

    <!-- Elevation for floating call-waiting banner -->
    <dimen name="call_waiting_elevation">8dp</dimen>

    <!-- Horizontal margin around the call-waiting banner -->
    <dimen name="call_waiting_margin">12dp</dimen>

    <!-- Accept / Reject button size inside the call-waiting banner -->
    <dimen name="call_waiting_action_btn_size">44dp</dimen>

    <!-- Padding inside the call-waiting banner card -->
    <dimen name="call_waiting_padding">14dp</dimen>

    <!-- Label text size for "Incoming Call" badge -->
    <dimen name="call_waiting_badge_text_size">11sp</dimen>

    <!-- Status text size inside the on-hold bar -->
    <dimen name="call_on_hold_text_size">14sp</dimen>

    <!-- Swap / Merge icon button size in the on-hold bar -->
    <dimen name="call_hold_action_btn_size">36dp</dimen>
</resources>

